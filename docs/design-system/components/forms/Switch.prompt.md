A toggle for a setting that applies immediately — never inside a form that has a Save button (use `Checkbox` there).

```jsx
<Switch label="Store is visible" description="Buyers can find your storefront." defaultChecked />
```
