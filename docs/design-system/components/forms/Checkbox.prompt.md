A checkbox with an optional label and description line.

```jsx
<Checkbox label="Save this address" description="For faster checkout next time." defaultChecked />
```

18px box, red fill when checked. For a set of mutually exclusive options use `RadioGroup`; for an instant on/off setting use `Switch`.
