The 208px dark rail that frames the seller dashboard (Overview / Subscriptions / Products / Orders / Settings).

```jsx
<SellerSidebar storeName="Ah Huat Roasters" activeHref="/seller/dashboard/products" onNavigate={go} />
```

Navy-900 ground, red left-edge marker on the active row — the seller surface is the one place BOMY goes dark by default.
