const { Button, Badge, Card, CardHeader, CardTitle, CardDescription, CardContent, ProductCard, BrandCard, Icon, Wordmark } = window.BOMYDesignSystem_030168
const ICONS = "../../assets/icons"

function Hero({ go }) {
  return (
    <section style={{ background: "var(--red-600)", color: "#fff", position: "relative", overflow: "hidden" }}>
      <div style={{ maxWidth: "var(--container-wide)", margin: "0 auto", padding: "var(--space-20) var(--space-8)", display: "grid", gridTemplateColumns: "1.15fr 1fr", gap: "var(--space-12)", alignItems: "center" }}>
        <div>
          <p style={{ margin: 0, font: "var(--type-eyebrow)", textTransform: "uppercase", letterSpacing: "var(--tracking-widest)", color: "var(--gold-300)" }}>Brands of Malaysia</p>
          <h1 style={{ margin: "var(--space-4) 0 0", font: "var(--type-display)", letterSpacing: "var(--tracking-tighter)", color: "#fff" }}>The home of authentic Malaysian brands.</h1>
          <p style={{ margin: "var(--space-5) 0 0", font: "var(--type-body-lg)", color: "var(--red-100)", maxWidth: "34rem" }}>
            We bring Malaysia's best-loved makers together under one trusted roof — for shoppers here and around the world.
          </p>
          <div style={{ display: "flex", gap: "var(--space-3)", marginTop: "var(--space-8)" }}>
            <Button variant="reward" size="lg" onClick={() => go("/products")}>Shop the marketplace</Button>
            <Button size="lg" variant="outline" style={{ background: "transparent", color: "#fff", borderColor: "rgba(255,255,255,.5)" }} onClick={() => go("/brands")}>Meet the brands</Button>
          </div>
        </div>
        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "var(--space-3)" }}>
          {[["gift", "Welcome Gift"], ["sparkles", "Quarterly Goodie Box"], ["truck", "Ships nationwide"], ["globe-alt", "MY & international"]].map(([icon, label]) => (
            <div key={label} style={{ background: "rgba(255,255,255,.1)", borderRadius: "var(--radius-panel)", padding: "var(--space-5)", display: "flex", flexDirection: "column", gap: "var(--space-3)" }}>
              <Icon name={icon} size={24} basePath={ICONS} style={{ color: "var(--gold-300)" }} />
              <span style={{ font: "var(--type-label)", color: "#fff" }}>{label}</span>
            </div>
          ))}
        </div>
      </div>
    </section>
  )
}

function Section({ title, action, onAction, children }) {
  return (
    <section style={{ maxWidth: "var(--container-wide)", margin: "0 auto", padding: "var(--space-16) var(--space-8) 0" }}>
      <div style={{ display: "flex", alignItems: "baseline", justifyContent: "space-between", marginBottom: "var(--space-6)" }}>
        <h2>{title}</h2>
        {action && <a href="#" onClick={(e) => { e.preventDefault(); onAction() }} style={{ font: "var(--type-body)" }}>{action}</a>}
      </div>
      {children}
    </section>
  )
}

function HomeScreen({ go }) {
  const d = window.BOMY_DATA
  return (
    <div>
      <Hero go={go} />
      <Section title="Fresh from our makers" action="Browse all products" onAction={() => go("/products")}>
        <div style={{ display: "grid", gridTemplateColumns: "repeat(4, minmax(0, 1fr))", gap: "var(--grid-gap)" }}>
          {d.products.slice(0, 4).map((p) => (
            <ProductCard key={p.id} name={p.name} storeName={p.store} category={p.category} price={p.price} iconBase={ICONS} href="#" />
          ))}
        </div>
      </Section>
      <Section title="Brands worth knowing" action="See all brands" onAction={() => go("/brands")}>
        <div style={{ display: "grid", gridTemplateColumns: "repeat(3, minmax(0, 1fr))", gap: "var(--grid-gap)" }}>
          {d.brands.slice(0, 3).map((b) => <BrandCard key={b.slug} {...b} href="#" />)}
        </div>
      </Section>
      <Section title="Why BOMY exists">
        <div style={{ display: "grid", gridTemplateColumns: "repeat(3, minmax(0, 1fr))", gap: "var(--grid-gap)" }}>
          {[
            ["Great products stay hidden.", "Brilliant local makers have the craft but rarely the marketing reach. Without exposure, their best work never finds the people who would love it."],
            ["Growth is too costly to go it alone.", "Reaching new customers — and breaking into wider markets — takes resources most small brands simply don't have."],
            ["Stronger together.", "Under one trusted umbrella, Malaysian brands gain the exposure, infrastructure, and audience they couldn't reach alone."],
          ].map(([t, b]) => (
            <Card key={t}><CardHeader><CardTitle>{t}</CardTitle><CardDescription>{b}</CardDescription></CardHeader></Card>
          ))}
        </div>
      </Section>
      <section style={{ maxWidth: "var(--container-wide)", margin: "var(--space-16) auto 0", padding: "0 var(--space-8)" }}>
        <div style={{ background: "var(--surface-reward)", border: "1px solid var(--gold-200)", borderRadius: "var(--radius-panel)", padding: "var(--space-12)", display: "flex", alignItems: "center", justifyContent: "space-between", gap: "var(--space-8)" }}>
          <div>
            <Badge variant="reward">Platform membership</Badge>
            <h2 style={{ marginTop: "var(--space-3)" }}>Join the community backing local.</h2>
            <p style={{ marginTop: "var(--space-2)", font: "var(--type-body-lg)", color: "var(--text-muted)", maxWidth: "38rem" }}>
              A Welcome Gift, a quarterly Goodie Box from our brand partners, early access to new drops, and member-only vouchers.
            </p>
          </div>
          <Button variant="reward" size="lg" onClick={() => go("/membership")}>Join for RM75/yr</Button>
        </div>
      </section>
    </div>
  )
}
Object.assign(window, { HomeScreen })
